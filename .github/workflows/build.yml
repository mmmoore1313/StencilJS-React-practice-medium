name: Build on push

on:
  push:
  pull_request:

jobs:
  build:
    name: Build Package
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@master
      - name: Build
        run: npm run build
